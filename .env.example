# Copy to .env (gitignored) and export into your shell, or set these
# directly in your environment. Env vars override config.yaml.

FINPIPE_DATA_DIR=data
FINPIPE_GOOGLE_SHEET_ID=
GOOGLE_APPLICATION_CREDENTIALS=
